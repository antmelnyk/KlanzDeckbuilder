<!DOCTYPE html>
<html>
  <head>
    <title>KlanzParser</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Навигация</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to 'Klanz Deckbuilder', root_path, :class => 'navbar-brand'  %>
      </div>

      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li class=<%= "active" if current_page?(decks_path)%>><%= link_to 'Деки', decks_path, :class => 'navbar-link'  %></li>
          <li class=<%= "active" if current_page?(cards_path)%>><%= link_to 'Расписание коллекционок', cards_path, :class => 'navbar-link'  %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Новенький? <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="http://klanz.ru/">В город!</a></li>
              <li role="separator" class="divider"></li>
              <li><a href="http://klanz.ru/pages/help">Путеводитель для новичка</a></li>
              <li><a href="http://klanz.ru/pages/help">Стратегия и тактика</a></li>
            </ul>
          </li>
        </ul>
        <form class="navbar-form navbar-left">
          <div class="form-group">
            <input type="text" class="form-control" placeholder="Search">
          </div>
          <button type="submit" class="btn btn-default">Submit</button>
        </form>
        <ul class="nav navbar-nav navbar-right">
          <li><a href="#">Мои деки</a></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Аккаунт <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <% if user_signed_in? %>
                  <li id="email"><strong><%= current_user.username %></strong></li>
                  <li><%= link_to 'Редактировать профиль', edit_user_registration_path, :class => 'navbar-link' %></li>
              <% else %>
                  <li><%= link_to 'Зарегистрироваться', new_user_registration_path, :class => 'navbar-link'  %></li>
                  <li><%= link_to 'Войти', new_user_session_path, :class => 'navbar-link'  %></li>
              <% end %>
              <% if user_signed_in? %>
              <li role="separator" class="divider"></li>
              <li><%= link_to 'Разлогиниться', destroy_user_session_path, method: :delete, :class => 'navbar-link'  %></li>
              <% end %>
            </ul>
          </li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>
  <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
  <% end %>
  <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
  <% end %>
  <%= yield %>
  </body>
</html>
